name: t_pdf_core_engine
description: "High-performance PDF core engine for Dart and Flutter, powered by PDFium via Dart FFI."
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.18.0
  ffi: ^2.1.4
  image: ^4.8.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
